{% extends _layout %}

<twig:block name="body">
    {% apply markdown_email %}
        # Hey {{ user.username }},

        Quelqu'un essaie de se connecter à votre compte sans succès depuis un instant,
        avez-vous oublié votre mot de passe ?

        Nous avons bloqué l'accès à votre compte pour votre sécurité, pour réactiver votre compte, veuillez suivre le lien ci-dessous :

        <twig:DashliteEmailButton title="Déverrouiller mon compte" url="{{ url('authentication_login_attempts_reset', {token: user.resetLoginAttemptsToken}) }}" :format="_format" />

        Pour plus de sécurité nous vous recommandons de réinitialiser votre mot de passe :

        <twig:DashliteEmailButton title="Réinitialiser mon mot de passe" url="{{ url('auth_reset_password_request') }}" :format="_format" />
    {% endapply %}
</twig:block>
