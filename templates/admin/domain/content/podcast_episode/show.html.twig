{% extends '@admin/shared/layout/base.html.twig' %}
<twig:block name="title">{{ "Podcast #{data.name}" }}</twig:block>

<twig:block name="header">
    <twig:Header title="{{ data.name }}" backPath="{{ path('admin_content_podcast_episode_index') }}" backTitle="Tous les podcasts">
        <twig:HeaderLinkButton name="Éditer" path="{{ path('admin_content_podcast_episode_edit', {id: data.id}) }}" icon="edit-alt-fill" />
        <twig:DashliteDeleteButton path="{{ path('admin_content_podcast_episode_delete', {id: data.id}) }}" :item="data" redirect="{{ path('admin_content_podcast_episode_index') }}" type="button" />
    </twig:DashliteHeader>
</twig:block>

<twig:block name="content">
    <twig:AsideCard>
        <twig:block name="tabs">
            <ul class="nav nav-tabs nav-tabs-mb-icon nav-tabs-card">
                <li class="nav-item">
                    <a class="nav-link active" href="#">
                        <span>Détails</span>
                    </a>
                </li>
                <li class="nav-item nav-item-trigger d-xxl-none">
                    <a href="#" class="toggle btn btn-icon btn-trigger" data-bs-target="userAside">
                        <twig:Icon name="user-list-fill"/>
                    </a>
                </li>
            </ul>
        </twig:block>

        <twig:block name="content">
            <div class="nk-block">
                <div class="nk-block">
                    <div class="nk-block-head">
                        {{ data.content | markdown_to_html }}
                    </div>
                    <twig:DataList>
                        <twig:DataItem label="Titre" value="{{ data.name }}" />
                        <twig:DataItem label="Slug" value="{{ data.slug }}" />
                    </twig:DashliteDataList>
                </div>
                <div class="nk-divider divider md"></div>
                <div class="nk-block">
                    <twig:DataList title="Informations supplémentaires">
                        <twig:DataItem label="Commentaires autorisés" :value="data.isCommentable | boolean" />
                        <twig:DataItem label="Featured" :value="data.isFeatured | boolean" />
                        <twig:DataItem label="Top promoted" :value="data.isTopPromoted | boolean" />
                        <twig:DataItem label="Premium" :value="data.isPremium | boolean" />
                    </twig:DashliteDataList>
                </div>
            </div>
        </twig:block>

        <twig:block name="aside">
            <div class="card-aside card-aside-right user-aside toggle-slide toggle-slide-right toggle-break-xxl toggle-screen-xxl" data-bs-content="userAside" data-toggle-screen="xxl" data-toggle-overlay="false" data-toggle-body="false">
                <div class="card-inner-group">
                    <div class="card-inner">
                        <div class="user-card user-card-s2">
                            <img src="{{ vich_uploader_asset(data, 'thumbnail_file') }}" alt="couverture {{ data.name }}">
                        </div>
                    </div>
                    <div class="card-inner"><h6 class="overline-title-alt mb-2">Additional</h6>
                        <div class="row g-3">
                            <div class="col-6">
                                <span class="sub-text">écoutes (unique)</span>
                                <span>4599</span>
                            </div>
                            <div class="col-6">
                                <span class="sub-text">écoutes (total)</span>
                                <span>9000</span>
                            </div>
                            <div class="col-6">
                                <span class="sub-text">Status</span>
                                {% if data.isOnline %}
                                    <span class="lead-text text-success">En ligne</span>
                                {% else %}
                                    <span class="lead-text text-danger">Hors ligne</span>
                                {% endif %}
                            </div>
                            <div class="col-6">
                                <span class="sub-text">Publication:</span>
                                <span>{{ data.scheduledAt | date }}</span>
                            </div>
                        </div>
                    </div>
                    {% if data.season %}
                        <div class="card-inner">
                            <h6 class="overline-title-alt mb-3">Saison</h6>
                            <span>
                                <a href="{{ path('admin_content_podcast_season_show', {id: data.season.id}) }}">
                                    {{ data.season.name }}
                                </a>
                            </span>
                        </div>
                    {% endif %}
                    {% if data.tags | length > 0 %}
                        <div class="card-inner">
                            <h6 class="overline-title-alt mb-3">Tags</h6>
                            <ul class="g-1">
                                {% for tag in data.tags %}
                                    <li class="btn-group">
                                        <a class="btn btn-xs btn-info btn-dim" href="#">{{ tag.name }}</a>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    {% endif %}
                    {% if data.technologies | length > 0 %}
                        <div class="card-inner">
                            <h6 class="overline-title-alt mb-3">Technologies</h6>
                            <ul class="g-1">
                                {% for technology in data.technologies %}
                                    <li class="btn-group">
                                        <a class="btn btn-xs btn-info btn-dim" href="#">{{ technology.name }}</a>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    {% endif %}
                </div>
            </div>
        </twig:block>
    </twig:DashliteAsideCard>
</twig:block>
