{% apply spaceless %}
    <!DOCTYPE html>
    <html lang="{{ app.request.locale }}" class="js">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="shortcut icon" href="{{ asset(devscast_dashlite.application.icon_path) }}">
        <title>Hors ligne - pas de connexion | {{ devscast_dashlite.application.name }}</title>
        <twig:block name="assets">
            {{ encore_entry_link_tags('admin') }}
            {{ encore_entry_script_tags('admin')}}
        </twig:block>
    </head>

    <body class="nk-body bg-white npc-default pg-error pattern">
    <div class="nk-app-root">
        <div class="nk-main ">
            <div class="nk-wrap nk-wrap-nosidebar">
                <div class="nk-content ">
                    <div class="nk-block nk-block-middle wide-xs mx-auto">
                        <div class="nk-block-content nk-error-ld text-center">
                            <h1 class="nk-error-head">Hors ligne</h1>
                            <h3 class="nk-error-title">Désolé nous n'arrivons pas à charger cette page...</h3>
                            <a href="/" class="btn btn-lg btn-primary mt-2">Rentrer sur terre</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </body>
    </html>
{% endapply %}
