<table class="subcopy" width="100%" cellpadding="0" cellspacing="0" role="presentation">
    <tr>
        <td>
            <div style="text-align: center">
                {{ this.title }}
                <img src="{{ this.src }}" title="{{ this.title }}" alt="qrcode" />
            </div>
        </td>
    </tr>
</table>
