<a {% for attr, value  in options %} {{ attr }}="{{ value }}"{% endfor %}>
<span class="float-right">
    {% if sorted %}
        {% if direction == 'desc' %}
            <twig:Icon name="arrow-up"/>
        {% else %}
            <twig:Icon name="arrow-down"/>
        {% endif %}
    {% else %}
        <twig:Icon name="opt-alt"/>
    {% endif %}
</span>
{{ title }}
</a>
